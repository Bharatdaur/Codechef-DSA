    //revesing the no.
     int ans=0;
     int x;
     cin>>x;

     while (x!=0)
     {
        int digit = x % 10;
        ans = (ans * 10) + digit;
        x = x / 10;
     }

     cout<<"The Reversed number is "<<ans<<endl;
     return 0;
